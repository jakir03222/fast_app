// // This is a basic Flutter  test.
// //
// // To perform an interaction with a  in your test, use the Tester
// // utility in the flutter_test package. For example, you can send tap and scroll
// // gestures. You can also use Tester to find child s in the
// // tree, read text, and verify that the values of  properties are correct.

// import 'package:flutter/material.dart';
// import 'package:flutter_test/flutter_test.dart';

// import 'package:ait_account/main.dart';

// void main() {
//   // tests('Counter increments smoke test', (Tester tester) async {
//     // Build our app and trigger a frame.
//     await tester.pump(const MyApp());

//     // Verify that our counter starts at 0.
//     expect(find.text('0'), findsOne);
//     expect(find.text('1'), findsNothing);

//     // Tap the '+' icon and trigger a frame.
//     await tester.tap(find.byIcon(Icons.add));
//     await tester.pump();

//     // Verify that our counter has incremented.
//     expect(find.text('0'), findsNothing);
//     expect(find.text('1'), findsOne);
//   });
// git config --global user.email "jakirhossian03222@gmail.com" ,
// git config --global user.name "jakir03222",
